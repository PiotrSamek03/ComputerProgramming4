---
server:
  port: 9999

spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ~
  h2:
    console:
      enabled: true
      path: /h2-console